# .env.example
# Purpose: Environment variable template

# Language: EXAMPLE

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# .github/workflows/ci.yml
# Purpose: GitHub Actions CI/CD pipeline configuration for project SnakeCaseName.

# Language: YAML

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# relative/path/file.ext
# Purpose: What this file does in one sentence.

# Language: EXAMPLE

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# index.html
# Purpose: Static frontend entry.

# Language: HTML/CSS/JS

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# styles.css
# Purpose: Static frontend styles.

# Language: CSS

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# app.js
# Purpose: Static frontend behavior.

# Language: JS/NodeJS

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences

# vercel.json
# Purpose: Vercel static build config.

# Language: JSON

# STRICT RULES:
# 1. Write COMPLETE, working code — no placeholders, no TODOs
# 2. Follow the API contract exactly
# 3. Use correct ENV variable names from the contract
# 4. Add proper error handling
# 5. Add JSDoc/docstrings for functions
# 6. For frontend: include loading states, error states, empty states
# 7. For backend: include input validation, rate limiting middleware
# 8. For database: use migrations format
# 9. Never hardcode secrets — use process.env or os.environ
# 10. Output ONLY the file content, no explanation, no markdown fences